[Unit]
Description=A lightweight DHCP and caching DNS server

[Service]
ExecStartPre=<%= @prefix || '/opt/dnsmasq' %>/sbin/dnsmasq --test
ExecStart=<%= @prefix || '/opt/dnsmasq' %>/sbin/dnsmasq --keep-in-foreground
KillMode=process
Restart=always

[Install]
WantedBy=multi-user.target
